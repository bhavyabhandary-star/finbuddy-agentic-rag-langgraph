"""Agent Layer (Layer 2): the ReAct loop, Intent Router, LangGraph state machine.

See kickoff_prompt.md's "Architecture — six layers" and "The agent loop (ReAct...)"
sections, and build_prompt.md's Intent Router section.
"""
