"""Memory Layer (Layer 4).

Short-term only for this MVP, per kickoff_prompt.md: long-term memory is
explicitly deferred, not half-built. If added later: retrieve top-k relevant
memories by search, don't stuff full history into every prompt.
"""
