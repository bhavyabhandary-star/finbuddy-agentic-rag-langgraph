[pytest]
testpaths = tests
pythonpath = .
# --tb=short: a credential-touching call failing deep in a library (e.g. an
# HTTP client) can otherwise print that frame's local variables verbatim in
# the traceback -- this happened once during setup and exposed a value.
addopts = --tb=short
