__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/

# Vector store + ingested corpus artifacts (regenerate from source PDFs, don't commit)
chroma_data/
*.sqlite3

# MLOps artifacts (regenerate via training scripts + mlops/intent_router/data.py;
# the registry itself is also regenerable, not a source-of-truth to version)
mlops/intent_router/artifacts/
mlops/intent_router/registry_store/
mlruns/
mlflow.db

# Never commit real UPI signals, PDFs pulled from a private source, or credentials
data/raw_pdfs/
*.pem
*.key

.pytest_cache/
.coverage
